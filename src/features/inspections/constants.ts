export enum InspectionStatus {
  PENDING = "Pending",
  IN_PROGRESS = "In Progress",
  COMPLETED = "Completed",
}

export enum SyncState {
  PENDING = "pending",
  SYNCED = "synced",
  FAILED = "failed",
}
